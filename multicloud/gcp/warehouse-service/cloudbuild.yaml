steps:
  # Build the Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'europe-west1-docker.pkg.dev/${PROJECT_ID}/warehouse-repo/warehouse-service:${BUILD_ID}'
      - '-t'
      - 'europe-west1-docker.pkg.dev/${PROJECT_ID}/warehouse-repo/warehouse-service:latest'
      - '.'

  # Push the image with BUILD_ID tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'europe-west1-docker.pkg.dev/${PROJECT_ID}/warehouse-repo/warehouse-service:${BUILD_ID}'

  # Push the image with latest tag
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'europe-west1-docker.pkg.dev/${PROJECT_ID}/warehouse-repo/warehouse-service:latest'

# Specify the images to store in Artifact Registry
images:
  - 'europe-west1-docker.pkg.dev/${PROJECT_ID}/warehouse-repo/warehouse-service:${BUILD_ID}'
  - 'europe-west1-docker.pkg.dev/${PROJECT_ID}/warehouse-repo/warehouse-service:latest'

options:
  logging: CLOUD_LOGGING_ONLY

